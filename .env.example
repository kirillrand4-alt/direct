# Скопируйте этот файл в .env и заполните.
#   cp .env.example .env
# Как получить токен — README.md, шаг 3.

# OAuth-токен Яндекс Директа (обязательно)
YANDEX_DIRECT_TOKEN=

# Логин клиента. Нужен ТОЛЬКО агентствам — рекламодателю строку можно не трогать.
# YANDEX_DIRECT_CLIENT_LOGIN=

# 1 — работать с песочницей вместо боевого API (полезно, пока заявка не одобрена)
# YANDEX_DIRECT_SANDBOX=0
